---
description: List all tasks across agents
agent: build
---

Run `task list -a` to show all tasks across all agents.

For a specific agent: `task list -t <agent>`

This shows task IDs, statuses (○ pending, ◉ in progress, ◇ verify, ✓ done), priorities, and titles.
